# New: Root | Parent | Current | Child # layouts/mutatons/new

<% if @evolution %>
  <p>
  <fieldset><legend><b>New</b></legend>
    <%= link_to_unless_current "Root", new_evolution_mutation_path(@evolution) %>
    <% if @mutation %>
        | <%= link_to_unless_current "Past", new_mutation_child_path(:mutation_id => @mutation.id) %>
        | <%= link_to_unless_current "Current", new_mutation_current_path(@mutation) %>
        | <%= link_to_unless_current "Future", new_mutation_after_path(@mutation) %>
    <% end %>
  </fieldset>
  </p>
<% end %>
