# Clone: Current | Current uni # layouts/mutations/clone
<% if @mutation %>
  <p>
  <fieldset><legend><b>Clone</b></legend>
    <%= link_to_unless_current "Current",
      :controller => 'mutations', 
      :action => 'clone_current_set', 
      :id => @mutation.id %> |
    <%= link_to_unless_current "Current & Past", 
      :controller => 'mutations', 
      :action => 'clone_current_and_past_set', 
      :id => @mutation.id %>
  </fieldset>
  </p>
<% end %>
