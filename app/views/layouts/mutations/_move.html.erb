<% if @mutation %>
    <p>
    <fieldset><legend><b>Move</b></legend>
      <% if session[:move_mutation_id] %>
      <% else %>
        <%= link_to "Current", "..." %> |
        <%= link_to "Current & Past", "..." %>
      <% end %>
    </fieldset>
    </p>
<% end %>
