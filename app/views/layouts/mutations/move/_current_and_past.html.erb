<% if session[:mutation_to_clone_current_and_past] %>
  <% if @evolution || @mutation %>
    <ul>
    <fieldset><legend><b>Clone(<%= session[:mutation_to_clone_current_and_past] %>) & Past</b></legend>
      ...
    </ul>
    </fieldset>
  <% end %>
<% end %>
