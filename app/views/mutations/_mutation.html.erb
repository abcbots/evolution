
  <% if mutation == @mutation %>
    <b><%= link_to_unless_current mutation.id, mutation %></b>
  <% elsif mutation == @mutation_move %>
    <b><%= link_to_unless_current mutation.id, mutation %></b>
  <% elsif mutation == @mutation_clone %>
    <b><%= link_to_unless_current mutation.id, mutation %></b>
  <% elsif mutation == @mutation_move_uni %>
    <b><i><%= link_to_unless_current mutation.id, mutation %></i></b>
  <% elsif mutation == @mutation_clone_uni %>
    <b><i><%= link_to_unless_current mutation.id, mutation %></i></b>
  <% else %>
    <%= link_to_unless_current mutation.id, mutation %>
  <% end %>

