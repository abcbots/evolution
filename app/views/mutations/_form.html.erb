<% form_for ([@evolution, @mutation]) do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.hidden_field :mutation_id %>
  </p>
  <p>
    <%= f.hidden_field :evolution_id %>
  </p>
  <p><%= f.submit "Send" %></p>
<% end %>
