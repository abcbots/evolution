
<% if @mutation_move %>
[
  move <%= link_to_unless_current @mutation_move.id, @mutation_move %> to:
    [<i><%= link_to "uni", :action => 'move_to_move_uni', :id => @mutation.id %></i>]
    [<%= link_to "root", :action => 'move_to_root', :id => @mutation.id %>]

  <% unless @child_or_current %>

    [<%= link_to "parent", :action => 'move_to_parent', :id => @mutation.id %>]
    [<%= link_to "current", :action => 'move_to_current', :id => @mutation.id %>]
    
  <% if @mutation != @mutation_move.ancestors.first %>
    
    [<%= link_to "children", :action => 'move_to_children', :id => @mutation.id %>]
    [<%= link_to "child", :action => 'move_to_child', :id => @mutation.id %>]

  <% end %>
  <% end %>

    [<%= link_to "cancel", :action => 'cancel_move', :id => @mutation.id %>]
]
<% end %>

